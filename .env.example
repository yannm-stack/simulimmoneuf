# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL of THIS application (Simulimmoneuf Bridge). 
# Used by your external site (simulimmoneuf.fr) to send leads here.
APP_URL="https://ais-pre-cvpgcmoikkkho4ook3fv7k-649204832248.europe-west2.run.app"
CONTACT_EMAIL=contact@simulimmoneuf.fr
SMTP_HOST=ssl0.ovh.net
SMTP_PORT=465
SMTP_USER=contact@simulimmoneuf.fr
SMTP_PASSWORD=
# CRM_URL: The destination where THIS app will forward leads.
# (Currently pointing to your other AI Studio CRM app)
CRM_URL=https://ais-pre-olgpljin4bh4c35p6o4fot-649204832248.europe-west2.run.app/api/leads

# Optional: API Key for the destination CRM if it requires authentication
CRM_API_KEY=
